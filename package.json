{
  "name": "radaryum",
  "version": "6.1.0",
  "private": true,
  "description": "Accumulated industrial opportunity intelligence engine on Cloudflare Workers and D1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "db:migrate": "wrangler d1 migrations apply radaryum-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
