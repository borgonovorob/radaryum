{
  "name": "radaryum",
  "version": "2.0.0",
  "private": true,
  "description": "Live industrial opportunity correlation engine on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}