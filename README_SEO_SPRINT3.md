# Radaryum SEO Sprint 3 — Public Intelligence Pages

Version: `6.3.0-public-intelligence`

New public server-rendered routes:

- `/latest`
- `/trending`
- `/countries` and `/country/:slug`
- `/industries` and `/industry/:slug`
- `/signals` and `/signals/:slug`

The dynamic sitemap includes only detail pages with enough public evidence and source diversity. Thin pages use `noindex,follow`. No D1 migration or new secret is required.
